PARAM.element.ui.dna = class{
    constructor(param = {}){
        this.openLine = param.openLine || {
            dist: 50
        }
        this.edge = param.edge || {
            delay: 0.15
        }
    }
}